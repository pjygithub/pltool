{
    "application": {
        "single_instance_guid": "",//将其设置为非空字符串以禁止应用程序的多个实例。该字符串应该是唯一的。
        "dpi_aware": true // 将此选项设置为 false 只会影响创建期间的窗口大小。
    },
    "debugging": {
        "show_console": false, // 是否显示带有调试输出的命令行控制台。
        "subprocess_show_console": false, // 显示所有 Chrome 子进程的更多控制台。
        "log_level": "DEBUG4", // 应用程序级别日志记录（不要与php日志记录混淆），可能的值为：ERROR、WARNING、INFO、DEBUG、DEBUG1、DEBUG2、DEBUG3、DEBUG4。ERROR 级别只会记录严重错误，DEBUG4 是最详细的。
        "log_file": "debug(debugging).log" // 限制：只有一个应用程序实例可以同时记录到该文件，当应用程序的第二个实例启动时，它不会将消息写入日志文件。
    },
    "main_window": {
        "title": "电镀线生产资料查询工具Ver.1.0.8beta(202302012256-202310302233)",
        "icon": "logo.ico",
        "default_size": [ // 宽度和高度
            900,
            720
        ],
        "minimum_size": [
            800,
            600
        ],
        "maximum_size": [
            0,
            0
        ],
        "disable_maximize_button": false, // 是否禁用最大化按钮。您可以将此选项与minimum_size和maximum_size混合使用，从而允许您定义一个具有固定大小且无法以任何方式调整大小的窗口。
        "center_on_screen": true,  // 是否使窗口在屏幕上居中。如果将其设置为 false，操作系统将负责定位窗口。
        "start_maximized": true,  // 启动主应用程序窗口并最大化。
        "start_fullscreen": false, // 以全屏模式启动主应用程序窗口。
        "always_on_top": false,  // 将窗口放置在所有非最顶层窗口之上。即使窗口被停用，它也会保持其最顶层的位置。
        "minimize_to_tray": true, // 是否最小化到系统托盘 - 任务栏图标将被隐藏。
        "minimize_to_tray_message": "已最小化到托盘。" // 最小化后将显示在气球通知中的消息。
    },
    "popup_window": {
        "icon": "", // 您可以为弹出窗口设置不同的图标。如果设置为空，将使用主窗口图标。
        "fixed_title": "", // 弹出窗口的标题是根据<title> 网页上的元标记设置的，但是您可以为所有弹出窗口设置固定标题，在这种情况下元标题将被忽略。
        "center_relative_to_parent": true, // 相对于父窗口居中弹出窗口。
        "default_size": [
            1280,
            960
        ]
    },
    "web_server": {
        "listen_on": [
            "127.0.0.1",
            0
        ],
        "www_directory": "www",
        "index_files": [ // 被视为目录索引的文件列表。最多允许 32 个文件。
            "index.html",
            "index.php"
        ],
        "cgi_interpreter": "php/php-cgi.exe",  // 所有 CGI 脚本的 CGI 解释器，无论脚本扩展名如何。
        "cgi_extensions": [  // 具有这些扩展名的文件被视为 CGI。最多允许 32 个分机。
            "php"
        ],
        "cgi_temp_dir": "",
        "404_handler": "/error-404.php",
        "hide_files": []
    },
    "chrome": {
        "cache_path": "cache", // 磁盘上存储缓存数据的目录。如果设置为空字符串，将使用内存缓存，并且您的 cookie 和其他数据（如 HTML5 数据库）将不会在应用程序启动会话中保留。Chrome 每 30 秒左右将缓存数据刷新到磁盘。
        "command_line_switches": { // 有许多设置可以通过 Chrome 命令行开关进行自定义。这些开关只能在 settings.json 文件中以编程方式设置，方法是传递开关字典，并以开关名称作为键。开关名称不应包含“-”或“--”前缀，否则将被忽略。如果开关不需要值，则开关值可以是空字符串。这些开关仅为主浏览器进程设置。
            "lang": "zh_CN",
            "enable-media-stream": "", // 启用媒体流（WebRTC 音频/视频）。
            "enable-system-flash": "", //  启用 Flash
            "enable-speech-input": "", // 启用语音输入 (x-webkit-speech)。
            "disable-web-security": "", //  不强制执行同源策略。
            "proxy-server": "socks5://127.0.0.1:8888",
            "no-proxy-server": "", //
            "allow-cross-origin-auth-prompt": "",
            "allow-insecure-localhost": "",
            "ash-enable-unified-desktop": ""
        },
        "context_menu": {
            "enable_menu": true, // 是否启用上下文菜单。
            "navigation": true, // 是否显示“后退”、“前进”和“重新加载”选项。
            "print": false, //  是否显示“打印...”选项。
            "view_source": false, // 是否显示“查看源代码”选项。
            "open_in_external_browser": false, // 是否显示“在外部浏览器中打开页面”和“在外部浏览器中打开框架”选项。
            "devtools": true  // 是否显示“显示开发人员工具”选项。
        },
        "devtools_F12": true,
        "external_drag": true, // 是否允许外部拖动事件进入浏览器窗口。例如，拖动 url 并进行导航，或者拖动文件并显示其内容。
        "external_navigation": true, // 是否允许在 PHP 桌面浏览器内导航到外部网站。当设置为 false 时，外部链接将在操作系统默认浏览器中打开。此行为通常适用于外部导航，因此不仅在单击链接时触发，而且在进行重定向时也会触发
        "log_file": "debug(chrome).log", // 用于调试日志的目录和文件名。如果为空，则将使用默认名称“debug.log”，并将该文件写入应用程序目录。
        "log_severity": "default", // 日志严重性。仅记录此严重级别或更高级别的消息。还可以使用“log-severity”命令行开关进行配置，其值为“verbose”、“info”、“warning”、“error”、“error-report”或“disable”。默认为“信息”。
        "reload_page_F5": true, // 按 F5 键可以重新加载忽略缓存的页面。
        "remote_debugging_port": 0, // 用于远程调试的端口。接受 1024 到 65535 之间的值。值为 0 将生成随机端口。值为 -1 将禁用远程调试
        "enable_downloads": true
    }
}